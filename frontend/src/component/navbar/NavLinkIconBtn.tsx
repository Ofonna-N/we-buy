import { styled } from "@mui/material";
import Button from "@mui/material/Button";

const NavLinkIconBtn = styled(Button)(() => ({
  color: "white",
  textDecoration: "none",
}));

export default NavLinkIconBtn;
